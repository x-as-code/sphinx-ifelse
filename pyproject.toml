[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "sphinx-ifelse"
authors = [{name = "PhilipPartsch"}]
dynamic = ["version", "description"]

requires-python = ">=3.9,<4"
dependencies = [
  "sphinx>=7.4,<9",
]
